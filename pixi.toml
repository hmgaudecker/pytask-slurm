[workspace]
authors = ["Hans-Martin von Gaudecker <hmgaudecker@gmail.com>"]
channels = ["conda-forge"]
name = "pytask-slurm"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = ">=3.10"

[pypi-dependencies]
pytask-slurm = { path = ".", editable = true }
pytest = ">=9.0.2, <10"
